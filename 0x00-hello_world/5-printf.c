#include < stdio.h>
/* c programm that prints using printf */
/**
 * main -Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	printf("with proper gramer, but the outcome is a piece of art,\n);
	return(0);
}
