#include "main.h"
/*Write a function that adds two integers and returns the result.*/
/**
 * add - add two integers and return the result
 *
 * @y: first parameter for function
 * @z: secomd parameter for function
 * Return: always the sum of two digits
 */
int add(int y, int z)
{
	int a;

	a = y + z;
	return (a);
}
